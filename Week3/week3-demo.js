// WEEK 3 DEMO: 
/*
Task: write a function, walkToTreasure, that takes in x and y coordinates on a beach 
and “walks” to the location of the treasure buried in the sand. 
*/
//how many steps to get to trasure

function walkToTreasure(x, y, treasureX, treasureY) {
let steps=0
    // Find x ccordinate
    while (x != treasureX){}
 if (x < treasureX){
console.log ("Going right!");
x++;
 }
 else {
    console.log("Going left!");
    x--;
 }
 steps++;
}
    // Find y coordinate

    while (y != treasureY){
        if (y < trasureY)
    }
  
}

for (let treasure=0; treasure < 3 ; treasure++)
